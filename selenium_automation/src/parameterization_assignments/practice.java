package parameterization_assignments;

import java.io.FileInputStream;
import java.io.FileNotFoundException;
import java.io.IOException;

import org.apache.commons.collections4.bag.SynchronizedSortedBag;
import org.apache.poi.ss.usermodel.CellType;
import org.apache.poi.xssf.usermodel.XSSFCell;
import org.apache.poi.xssf.usermodel.XSSFRow;
import org.apache.poi.xssf.usermodel.XSSFSheet;
import org.apache.poi.xssf.usermodel.XSSFWorkbook;

public class practice {

	public static void main(String[] args)  {

		int a=20;
	//	a.tostring();		//System.out.println(a.toString()); 
		// cannot apply tostring to primitive object
		
		//applicable only to objets i  which data is stored
		//you cannot directly invoke the toString() method on primitive types like int, float, boolean, etc. 
		//This is because primitive types are not objects and don't have methods associated with them.
		
	}

}

